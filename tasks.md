Треба зробити:
14. Лишок CVE (потребує стрибка фреймворку — свідомо відкладено): 9 у `starlette`. Multipart-фікси
    (0.40.0/0.47.2) → **fastapi ≥ 0.118**; решта (Host-заголовок, StaticFiles, шлях) — лише
    starlette 1.x, тобто fastapi 0.12x. У нас nginx попереду фіксує Host, StaticFiles не
    використовується. Повернутись, коли 1.x відлежиться. Без фіксу взагалі: `python-jose`
    (jwe.decrypt — JWE не використовуємо) і `ecdsa` (Minerva, P-256 — підписуємо HS256); обидва
    зникнуть при переході на `PyJWT`.

1. Користувачі: можу тільки додати і видалити, не можу відредагувати

2. Довідники: Служби, не можу відредагувати якщо треба
   Заодно (виявлено в задачі 32): `DELETE /api/settings/services/{id}` без гарда —
   служба з майном впаде 500 (`nomenclature.service_id` = RESTRICT), а служба без майна
   тихо забере каскадом свої записи журналу підписантів (склад + начальник). Треба
   зрозумілий гард. Плюс після переходу начальника на журнал — прибрати з картки служби
   мертві `chief_name/chief_position` (лишились фолбеком, доки служби не заведені в журнал).

13. Прибрати мертвий код op_type_details (підтипи операцій): таблиця/модель/ендпоінти
    (`/api/settings/op-types-detail`) + api settings.js лишились, але після дропу v1 їх ніхто
    не використовує (нова вкладка «Типи операцій» — лише батьківський тип). Прибрати окремо.

7. Історія видач стійка до змін/видалення особи: додати assignment.person_name (ПІБ-знімок при видачі); історія показує цей знімок (а не резолвить наживо). FK assignment.person_id змінити з CASCADE на SET NULL, щоб при видаленні особи рядок видачі (і знімок ПІБ) лишався. Краєвий випадок: заборонити видалення особи, якщо в неї є активна видача (returned_date IS NULL). Міграція на person_name + зміну FK.

9. Поле номер картки число чи можемо записати текст?

11. Редагувати категорії (типи майна)




Готово:
49. Баг: у стрічці «Переміщення» в колонці «Картка» для видач показувався СЕРІЙНИЙ номер.
    Дві причини разом: (1) події видачі й зміни точки віддавали `card_number: None` — № картки
    живе на екземплярі, а в подію не підтягувався; (2) UI мав фолбек `card_number || serial_no`,
    тож у колонці з назвою «Картка» опинявся серійний. Тепер стрічка вантажить № карток пачкою
    поряд із серійними і проставляє їх подіям видачі/точки (в історії картки — те саме через
    `card_of`), а колонка показує **лише** № картки; серійний доступний у підказці рядка.
    Тести: у стрічці видача серійного має саме № картки, і подія точки теж. Гілка `fix/feed-card-number`.

48. Імпорт видач читає колонку **«Точка»** (заголовки «Точка» / «Точка зберігання»). Значення
    застосовується **незалежно від «Де»**: рядок із прізвищем → видача + точка, рядок лише зі
    складом/підрозділом → точка без видачі (раніше такі рядки просто пропускались). Куди лягає:
    серійне — на екземпляр, видане несерійне — на видачу, несерійне на складі — мітка
    (картка, склад); зміни пишуться в журнал подій точок, тож видно в загальній історії.
    Точки find-or-create для складу цього рядка (лічильники `points_created`/`points_set` у звіті).
    Склад рядка: підрозділ із «Де», а для «складу» — склад служби з колонки «Служба».
    Цикл імпорту перебудовано: резолв рядка (служба/картка/склад/екземпляр) відокремлено від
    створення видачі, тож помилка видачі (особу не знайдено, вже видано, бракує залишку) більше
    не з'їдає точку — вона все одно проставляється, а причина йде у звіт.
    Тест: e2e на 5 рядків (серійне видане/на складі, несерійне видане/на складі, рядок без точки)
    + реімпорт не плодить точок. Фікстури `import_v2_point_items/mv.xlsx`.
    Гілка `feat/import-storage-point`.

47. Supply chain: локи, хеші, digest-и (задача 14). Найбільша діра була не в CVE, а в тому, що
    **фронт збирався без `package-lock.json`** — `npm install` по діапазонах `^` тягнув будь-яку
    сумісну версію на момент білду, тобто скомпрометований реліз приїхав би сам.
    • **`frontend/package-lock.json`** + `npm ci` у Dockerfile. Версії підбирав за політикою
      витримки, а не «як вирішить npm»: `vue 3.5.41` вийшов ТОГО Ж ДНЯ, `axios 1.19.0` — тиждень
      тому, тож зафіксовано зрілі `vue 3.5.40` (20 дн.), `axios 1.18.1` (44 дн.), `pinia 2.3.1`,
      `vue-router 4.6.4`, `vite 5.4.21`. `npm audit --omit=dev` — 0.
    • **`backend/requirements.lock`** — усі 60+ транзитивних залежностей із SHA256, встановлення
      через `--require-hashes`: перевипущений артефакт із тією ж версією просто не встановиться.
      `requirements.txt` лишається джерелом правди; команда перегенерації — в Dockerfile.
    • **Docker-образи по digest** (node/nginx/python/postgres) — тег можна перевипустити з іншим
      вмістом, digest — ні. Команда оновлення записана в коментарі.
    • CI ставить бекенд із локу (`--require-hashes`), кеш ключується на нього ж.
    Перевірка: повний перебілд `--no-cache` (у логах видно `npm ci` і `--require-hashes`),
    46 pytest + 125 e2e зелені. Гілка `chore/supply-chain-locks`.

46. Оновлення залежностей під CVE — мінімально достатніми версіями, без стрибка фреймворку.
    Політика: беремо найнижчу версію, що закриває конкретну CVE (а не «останню»), і лише те, що
    досяжне в нашій моделі загроз; витримка релізу ≥2-4 тижні (перевірено: 62-533 дні); по одному
    пакету на коміт, щоб `git bisect` мав сенс. Оновлено: **python-multipart 0.0.9 → 0.0.31**
    (7 CVE у парсингу форми — це публічний, до-авторизаційний вектор на логіні; ставиться під
    наш fastapi 0.111 без конфлікту), **python-jose 3.3.0 → 3.4.0** (algorithm confusion + DoS),
    **python-dotenv → 1.2.2**, **pytest → 9.0.3** (dev). Результат: **24 → 16 CVE**; лишок і
    причини — у задачі 14. Перевірка: 46 pytest + 125 e2e зелені на оновлених залежностях.
    Гілка `chore/deps-cve-minimal`.

45. Перевірка безпеки: CI-сканери + автотест авторизації + жорсткі місця.
    • **`.github/workflows/security.yml`** (PR / main / щопонеділка): `pip-audit`, `npm audit`,
      `bandit`, `gitleaks` (з повною історією), `trivy fs`. Поки **неблокуючі**
      (`continue-on-error`) — спершу треба розгребти наявний борг.
    • **`api_auth_sweep`** — проходить по OpenAPI-схемі й б'є КОЖЕН маршрут без токена (401/403),
      з лічильником перевірених маршрутів, щоб тест не пройшов вхолосту; окремо — admin-only
      маршрути проти service-юзера. Дірок не знайдено.
    • **Ліміти імпорту** (`read_xlsx_upload` в `import_helpers.py`): 15 МБ на файл і 300 МБ на
      розпакований вміст — XLSX це zip, тож маленький файл міг розкластись у гігабайти й покласти
      бекенд ще до першого рядка.
    • **Стримування підбору пароля**: лічильник невдалих спроб на пару логін+IP у вікні 5 хв,
      11-та → 429; успішний вхід скидає. У пам'яті процесу (бекенд — один контейнер).
    **Перший результат сканерів:** `bandit` — 0 середніх/високих (1 low), `npm audit --omit=dev`
    — 0; `pip-audit` — **24 CVE у 6 пакетах**: `starlette 0.37.2` (9), `python-multipart 0.0.9` (7),
    `python-jose 3.3.0` (5), `ecdsa`, `python-dotenv`, `pytest`. Оновлення залежностей — окрема
    задача (тягне fastapi і перевірку `passlib`+`bcrypt` pin). Гілка `feat/upload-and-login-guards`.

44. Баг порядку історії: видача того ж дня опинялась ПІД переміщенням, якщо рух мав номер
    накладної. Ключ сортування ставив `doc_sort_key` перед `created_at`, тож документований рух
    (непорожній кортеж номера) завжди вигравав у видачі (порожній кортеж) — задача 30 лікувала
    лише випадок, коли номера не було в обох. Тепер спільний `history_sort_key()`
    (`custody_snapshot.py`) додає ранг джерела: того самого дня спершу рухи (між собою — за
    НОМЕРОМ накладної, бо в імпорті лише він дає хронологію), а події особи/точки (видано,
    повернуто, зміна точки) — після них, тобто зверху у стрічці. Застосовано і в історії картки,
    і в загальній стрічці `/custody/feed`. Тест: видача над документованим переміщенням + дві
    накладні того ж дня далі впорядковані номером (задача 10 не зламана).
    Побічно: у тесті точки для виданого прибрано фальшиву синхронізацію (`toHaveValue` після
    `selectOption` проходить одразу) — тепер чекаємо на саму відповідь PUT; 5 прогонів поспіль
    без флейків, повна сюїта — 121 passed двічі. Гілка `fix/history-order-documented-issue`.

43. Стабільність e2e + баг «Діє по» при створенні підписанта. Сюїта періодично «моргала» (то
    `audit_ui`, то Реквізити, то фін-МВО-тести) — усі падіння впирались у СПІЛЬНИЙ стан під
    паралельним запуском. Полагоджено по-різному, за причиною:
    • **фін-МВО** — сінглтон «один діючий на систему»: тести-споживачі підписанта більше за нього
      не змагаються, а беруть власне ЗАКРИТЕ вікно дат у минулому (`finWindow()` у `helpers/mvo.js`)
      і датують свої рухи/документи всередині нього; `closeActiveFin` лишився тільки в тесті самого
      правила «один діючий»;
    • **`audit_ui`** — журнал спільний, тож «перший рядок» нічого не гарантує: тепер шукаємо рядок
      за двома фільтрами (Служба + Створено), а не за позицією;
    • **Реквізити** — заповнення форми до того, як прийшли збережені значення, перетиралось
      відповіддю: додано очікування завантаження.
    Побічно знайдено **баг застосунку**: у формі підписанта поле «Діє по» при СТВОРЕННІ не
    надсилалось (`createMvo` слав лише `from_date`), тож запис завжди створювався діючим і ловив
    409 «вже є діючий МВО фінслужби» — користувач заповнював поле, а воно мовчки губилось.
    Перевірка: три повні прогони поспіль — 120 passed, 0 failed. Гілка `fix/test-isolation-singletons`.

42. Зміна точки — подія загальної історії (задача 12, частина 3; закриває задачу). Свідомо НЕ
    виводимо з `audit_log` (той журнал технічний і буде перероблятись) — заведено доменний журнал
    **міграція 032** `point_events`: дата, картка, екземпляр/видача/лише картка, склад,
    з_точки→в_точку (+**знімки назв**, щоб подія читалась після видалення точки з довідника),
    к-сть, хто/коли. Пишуть усі три шляхи зміни точки через `log_point_change()`
    (`point_events.py`): екземпляр (`PUT instances/{id}`), мітка залишку (`PUT /custody/stock-point`),
    видача (`PUT /assignments/{id}/point`); повторна установка тієї самої точки події не створює.
    Показ: `/custody/feed` (чип «Точка») і історія картки майна (`HistoryTimeline`) — «склад: Бокс 1
    → Намет». Скоуп ролі — новий `scope_point_events` (service → своя служба, mvo → свій склад).
    Стосується і обліку, і НДМ. Тести: e2e API (2 події серійного + ідемпотентність + історія картки
    + несерійне: мітка й точка видачі з к-стю) та UI-smoke чипа «Точка». Гілка `feat/point-events`.

41. «Переміщення» — загальна історія (задача 12, частина 1-2). Сторінка показувала лише рухи
    складів; тепер це єдина хронологічна стрічка **рухи + видачі/повернення** — те саме, що в
    картці майна, але по всій частині. Бек: `GET /api/custody/feed` (склад, `date_from`/`date_to`,
    `limit`=500) — скоуп ролі через `scope_movements`/`scope_assignments`, довідники вантажаться
    пачкою (без N+1), сортування тим самим ключем, що й історія картки (дата → номер накладної →
    `created_at` → id). Видача дає до ДВОХ подій (видано/повернуто), тому дати фільтруються по
    кожній події окремо, а SQL лише грубо звужує вибірку. Фронт: фільтри склад + період + чипи
    типів подій із лічильниками, колонка «Звідки → Куди / Кому», у підвалі — скільки показано з
    усього. Тести: e2e API (злиття, порядок, фільтри складу/дат, скоуп чужої служби) + UI-smoke.
    Гілка `feat/movements-feed`. **Не закрито:** зміна точки зберігання ще не подія стрічки.

40. Точка й для ВИДАНОГО майна. Серійне: екземпляр і після видачі числиться на складі, тож точка
    живе на `instances.storage_point_id` — прибрано лише блокування в UI (таблиця + картка).
    Несерійне: мітка (картка, склад) спільна на весь залишок і для видачі не годиться, тому
    **міграція 031** — `assignments.storage_point_id` (SET NULL) + `PUT /api/assignments/{id}/point`
    (гард: точка того самого складу; для серійної видачі — 400 «ставиться на екземплярі»; ACL як
    на видачу). `GET /assignments` віддає `storage_point_id`/`storage_point`. Фронт: пікер точки
    тепер у КОЖНОМУ рядку «Залишків», `savePoint` розгалужується екземпляр / залишок картки /
    конкретна видача. Тести: e2e API (точка видачі не чіпає мітку залишку, чужа точка 400, null
    прибирає, серійне — через екземпляр) + UI-smoke на вкладці «Видане».
    Гілка `feat/issued-storage-point`.

39. «Залишки»: **№ картки** першою колонкою. Бек `/custody/serial` тепер віддає `card_number`
    (у «Де знаходиться» він уже був); рядок таблиці показує його моно-шрифтом, несерійне — «—».
    Заодно: пошук на «Залишках» шукає й за номером картки, а картка екземпляра показує його
    поряд із серійним. `InstanceCreate` отримав `card_number` — раніше при ручному створенні
    екземпляра номер картки задати було нічим (тільки імпорт/приймання). Тест: `stock_note_ui`
    перевіряє першу колонку і № картки в модалці. Гілка `feat/stock-card-number`.

38. Точку можна ставити на будь-якому складі (задача 3). Технічно це працювало й раніше — у
    Довідниках у формі точки доступні ВСІ склади, — але на «Залишках» пікер не рендерився взагалі,
    якщо для складу ще не заведено жодної точки (`v-if="…&& points.length"`), тож на складі
    підрозділу поле виглядало нередагованим. Тепер пікер показується завжди (для того, що на
    складі), а в ньому є **«+ нова точка…»**: prompt із назвою → точка створюється **для поточного
    складу** і одразу проставляється рядку. Те саме в картці екземпляра (проп `create-point`).
    Створення — лише admin (ендпоінт `require_admin`), не-адміну опція просто не показується.
    Прив'язку точки до складу лишили: саме на ній тримається гард «чужа точка» і скидання при
    переміщенні. Тест: UI-smoke — на складі підрозділу без точок створити точку інлайн, перевірити
    що вона зʼявилась саме в цього складу і проставилась картці. Гілка `feat/inline-storage-point`.

37. Картка екземпляра на «Залишках» (задача 4). Примітка й раніше редагувалась інлайн у колонці,
    але поле було замасковане: рамка лише на hover, порожнє значення показувалось як «—», а після
    появи колонки «Точка» таблиця стала ширшою і колонка виїжджала за край. Тепер серійний рядок
    має кнопку **«Картка»** → модалка `InstanceCardModal`: серійний №, тип, склад, на кому (read-only)
    + **Точка** і **Примітка** нормальними полями з «Зберегти» (правки застосовуються лише на
    збереження). У таблиці «Примітка» стала read-only текстом з обрізанням і `title`; інлайн-вибір
    точки лишився для швидкої масової розстановки. Тести: `stock_note_ui` переписано під картку
    (+перевірка серійного в картці й тексту в колонці), у `stock_point_ui` — що картка показує ту
    саму точку. Гілка `feat/instance-card-modal`.

36. Баг: звіт «Видане на групу» був порожній навіть коли групі призначено командира.
    `groups.commander_id` (хто командир) і `persons.group_id` (членство) ніде не звʼязувались,
    а звіт бере членів лише за `persons.group_id` — тож група з командиром мала НУЛЬ членів.
    Тепер: (1) призначення командира при створенні/редагуванні групи проставляє йому членство
    (`_join_commander`); (2) **міграція 030** — бекфіл для вже створених груп (лише командирам
    БЕЗ групи, свідоме членство не переписуємо); (3) звіт показує командира завжди, навіть якщо
    в його картці інша група; (4) порожня група тепер каже «У групі немає бійців — призначте
    групу особам у Довідники → Особи» замість порожнечі. Тести: e2e API (командир стає членом,
    зміна командира, порожня група) + UI-smoke вкладки «Видане на групу».
    Гілка `feat/group-commander-membership`.

35. Особи в UI: єдиний підпис «Прізвище Ім'я (Позивний)» + сортування (задача 10). `personLabel`
    був продубльований у 4 сторінках із різною поведінкою (позивний показувався лише коли ПІБ
    порожній, порядок — як прийшло з БД). Винесено у `frontend/src/utils/person.js`:
    `personLabel` (з позивним), `personFullName` (лише ПІБ — для таблиці Особи, де позивний уже
    окремою колонкою), `sortedPersons`, `personOptions` (для `ItemAutocomplete`: позивний у назві,
    ІПН у меті). Звіти → «Видане на особу»: `<select>` замінено на **пошуковий пікер**
    (`ItemAutocomplete`). Підключено скрізь: Довідники (пікер підписанта, командир групи, таблиця
    Особи), Залишки (видача, пікер особи при переміщенні, «На кому»), Користувачі (прив'язка
    особи до логіну). Тест: оновлено `reports_ui` (пікер замість select, пошук за позивним,
    формат підпису). Гілка `feat/person-picker-format`.
    **Доповнено:** усі решта дропдаунів осіб теж стали пошуковими пікерами — командир групи
    (Довідники → Групи), «Кому» в модалці «Видати» на Залишках, «Особа» в Користувачах;
    порожній ввід скидає вибір (поля опційні). Тести: командир у `structure_ui`, хелпер
    `pickPerson` у `assignments_ui`/`ndm_ui`/`stock_serial_return_ui`.
    Гілка `feat/person-pickers-everywhere`.

34. Точки зберігання — фізичне розміщення всередині складу (майно обліково лишається на
    ОДНОМУ складі, з нього ж іде в документи). Міграція **029**: довідник `storage_points`
    (назва + склад, назва унікальна в межах складу), `instances.storage_point_id` (SET NULL)
    і `nomenclature_points` (позначка для несерійного — одна на пару картка+склад, БЕЗ
    розподілу кількості; розподіл к-сті по точках вимагав би окремого леджера — свідомо ні).
    Довідкова вісь: баланси, накладні, ACL не змінюються. **Переїзд на інший склад скидає
    точку** — вона належала старому складу; щоб жоден шлях (рух/накладна/відкат/імпорт) цього
    не забув, усі присвоєння `current_warehouse_id` заведено в `place_instance()`
    (`custody_placement.py`). Гарди: точка має бути з того ж складу (400), для серійної картки
    позначка несерійного не ставиться (400). Бек: `/api/structure/storage-points` CRUD,
    `PUT /api/custody/stock-point`, точка в `/custody/serial`, `/custody/balances`, `/custody/where`.
    Фронт: вкладка **«Точки зберігання»** в Довідниках (назва+склад, ✎/✕) і колонка **«Точка»**
    на «Залишках» з інлайн-вибором + фільтр (усі / конкретна / без точки); редагується лише
    для того, що на складі (видане на руках — точки не має). Тести: e2e API (CRUD+унікальність,
    серійне, чужа точка, скидання при переміщенні, несерійна позначка, видалення точки не чіпає
    майно) + UI-smoke. Гілка `feat/storage-points`.

33. Баг: «Відкликати» рух на «Документи → Без документа» відкочував розміщення, але
    **лишав активну видачу** — майно поверталось на попередній склад і водночас світилось
    як видане особі (видача створюється разом із рухом у «Додати переміщення», а `assignments`
    не мають звʼязку з `custody_movements`). Старий гард цього не ловив: видача НЕ рухає
    баланс, тож `bal - qty < 0` не спрацьовував. Тепер `DELETE /custody/movements/{id}`:
    серійне — 400, якщо по екземпляру є активна видача; несерійне — 400, якщо після відкату
    залишок складу-отримувача став би меншим за видане (`issued_qty`), тобто часткове
    відкликання поверх виданого лишається дозволеним. Повідомлення: «спершу оформіть
    повернення». Тест e2e (серійне блок→повернення→ок; несерійне 5−2 при 3 виданих — можна,
    3−3 — ні). Гілка `fix/revoke-blocked-by-issue`.

32. Начальник служби — підписант із журналу; вкладку «МВО» перейменовано на **«Підписанти»**.
    Слот начальника в Дод.25 існував і раніше, але брався з вільного тексту
    `services.chief_name/chief_position` — без історії (стара накладна показувала
    ПОТОЧНОГО начальника). Тепер це третій вид запису журналу: `kind='service_chief'`
    + `mvo.service_id` (міграція **028**), один діючий на службу, з періодом дії;
    `mvo_at(..., service_id=)` резолвить його на дату документа, snap заморожує при
    підписанні. **Фолбек** на `services.chief_*` лишено, доки службу не завели в журнал
    (бекфіл неможливий: вільний текст без звʼязку з `persons`). Звання начальника не
    друкуємо — у Дод.25 для нього лише посада+ПІБ. Модель/таблицю `Mvo` НЕ перейменовано
    (зачепило б audit_log/імпорти) — перейменовано лише UI: вкладка, колонка «Підписує за»,
    тип «Начальник служби» з вибором служби; у формі документа блок «Підписанти» отримав
    4-те поле «Начальник служби». Тести: e2e API (журнал по службі + історичний резолв за
    датою + фолбек + 409 на другого діючого + 400 без служби) + UI-smoke створення.
    Гілка `feat/service-chief-signatory`.

31. Документ: «Служба» більше не вибирається — виводиться з майна документа (служба
    номенклатури позицій), і «Передає»/«Приймає» виведено в «Реквізити» (як рядок 15
    Дод.25). Бек: `derive_service_id` у `custody_snapshot.py` проставляє службу, якщо
    позиції однієї служби; різні служби (склад підрозділу тримає майно кількох служб)
    → поле лишається порожнім, без помилки. Кличеться у create/update документа,
    у `/receive` (служба з КАРТКИ, а не зі складу) і в бекфілі документів при **імпорті**
    — раніше імпортовані накладні друкувались із порожньою «Службою забезпечення».
    Побічно полагоджено: `_apply_group` читав `doc.movements` ДО лінкування → колекція
    кешувалась порожньою, тож snap щойно створеного документа лишався без «підсумків
    прописом» (`db.expire(doc, ['movements'])` перед snap). Фронт: Служба/Передає/Приймає
    — read-only `.ro-val`; при кількох службах у позиціях — «різні служби — не друкується»
    (`service_id` доданий у `_line_dict`); `service_id` прибрано з payload форми.
    Тести: e2e API (деривація + payload ігнорується + мікс-служби + PUT + /receive),
    +службa в бекфілі імпорту (`api_import_link`), UI-smoke реквізитів.
    Гілка `feat/doc-service-derive`.

30. Історія майна: сортування однодатних недокументованих подій. При «переміщенні з видачею»
    рух і видача мають одну дату й без номера накладної → тайбрейкер падав на `source_id`
    (movement.id проти assignment.id — різні послідовності, порядок випадковий; рух вилазив над
    своєю ж видачею). Додано `created_at` у ключ сортування ПІСЛЯ `doc_sort_key` (імпорт однієї
    дати далі впорядковується номером накладної — задача 10; недокументовані однодатні — за часом
    запису: видача флашиться після руху → зверху). Тест: transfer-with-issue → issued·transfer·receipt.
    Гілка `fix/history-same-date-order`.

29. Посада/звання перенесено з картки Особи на записи журналу МВО (потрібні лише підписантам
    документів; тепер snap бере посаду/звання на ПЕРІОД того призначення — лягає на резолв-по-даті
    із задачі 23). Міграція 027: `mvo.position/rank` + бекфіл активних записів із особи, дроп
    `persons.position/rank` + мертвих `*_genitive`. `mvo_person_at`→`mvo_at` (повертає запис МВО),
    snap читає посаду/звання з нього. Фронт: інпути посада/звання в модалці МВО (+ колонка), прибрано
    з модалки Особи; пікер осіб у StockPage → мета позивний/ІПН. Тест `api_doc_mvo_from_journal`
    ставить посаду/звання на МВО й перевіряє snap_*_post/snap_recv_rank. Гілка `feat/mvo-position-rank`.

28. ДРОП V1. Повністю прибрано стару систему обліку (items/movements/documents/residues/
    splits/recipients). 3 гілки: (1) `refactor/extract-shared-helpers` — спільні хелпери у
    нейтральні модулі `nakladna_common.py`/`import_helpers.py`, жоден v2-файл більше не імпортує
    v1; (2) `feat/remove-v1-frontend` — видалено сторінки items/movements/documents/residues +
    осиротілу /settings, компоненти ItemHistoryModal/IssueModal/RecipientAutocomplete, api-модулі
    (items/movements/documents/residues/splits/recipients/invoices/admin), маршрути; **«Реквізити»
    та «Типи операцій» перенесено у Довідники** (бек `/api/settings` не змінювався); (3)
    `feat/remove-v1-backend` — роутери/моделі/схеми v1 + **міграція 026** (DROP 8 таблиць, CASCADE,
    НЕЗВОРОТНО — потрібен pg_dump перед деплоєм). Збережено: `unit_settings`, `op_types`,
    `op_type_details`(поки), `invoice_export`. Дані реквізитів/типів операцій ПЕРЕЖИВАЮТЬ дроп.
    Побічно: полагоджено ізоляцію тестів (api_import_assignments_case дістав власні фікстури
    …ASGC/Авдєєнко, дані з рядка 3 — імпортер рухів читає з min_row=3). Перевірено: 46 pytest +
    104 e2e (двічі) + прод-збірка фронта (Rollup) зелені.

27. Імпорт видач: тепер шукає особу серед ІСНУЮЧИХ (не створює — люди імпортуються окремо). По прізвищу: спершу вже в цьому підрозділі, інакше — серед тих, у кого підрозділ НЕ заданий → таким проставляється підрозділ (`persons_unit_set`). Не знайдено → пропуск+звіт «імпортуйте людей»; неоднозначно → пропуск. Прибрано `persons_created`/створення. Тести: оновлено `api_import_assignments` (Петренко пре-існує без підрозділу → знаходиться, підрозділ проставляється, без дублю) + `api_wipe_persons` (пре-створення Петренко). Гілка `feat/assignments-find-existing`.

26. МВО: (A) «Очистити людей» тепер зберігає й осіб-МВО (об'єднання user-linked ∪ осіб журналу `mvo`), щоб журнал не осипався каскадом і не вносити заново (у звіті `kept_mvo`). (B) Довідники → МВО: повне **редагування** запису (особа+дати; `MvoUpdate` розширено, `update_mvo` застосовує person_id/from_date/to_date з валідацією періоду) + **видалення** (`DELETE /api/structure/mvo/{id}`). Фронт: у рядку кнопки ✎/✕, поле «Діє по», `openEditMvo`/`delMvo`. Тести: e2e API (edit+delete+гард періоду) + розширено gated wipe-persons (особа-МВО й запис журналу переживають wipe). Гілка `feat/mvo-edit-delete`.

25. Форма МВО: дропдаун особи — тепер **пошуковий** (`ItemAutocomplete`, як для номенклатури) + особи **відсортовані** (за ПІБ), у меті позивний/ІПН. Раніше був звичайний `<select>` з усіма особами (незручно після імпорту людей). `personOptions` (sorted); обгортка `.ac-field`. Оновлено `fin_mvo_ui` під autocomplete. Гілка `feat/mvo-person-picker`.

24. Імпорт людей (сторінка Імпорт, блок «Люди»). Колонки за заголовками: **ПІБ** (одна, розділення пробілом → прізвище/ім'я/по-батькові), **ІПН**, **Позивний**. Ключ — ІПН: є в базі → оновити (ПІБ/позивний/active); нема → створити; ІПН у базі без рядка у файлі → **деактивувати** (`is_active=False`); особи без ІПН не чіпаються. Рядок без ІПН — пропуск+звіт. `search_name` = повний ПІБ (нижній регістр) → покращує матчинг МВО з J/K (формат «Прізвище Ім'я По-батькові»). Ендпоінт `POST /api/admin/v2/import/persons`. Тест e2e (оновлення/створення/деактивація/пропуск). Гілка `feat/import-persons`.

23. МВО в документах — Задача 2 (фазами 2a/2b/2c). Підписанти Здав/Прийняв/Фін тепер РЕЗОЛВЛЯТЬСЯ з журналу МВО за `doc_date` (не FK). (2a) `mvo_person_at(scope,date)` + `snap_nakladna` резолвить 3 підписантів (Здав=МВО from-складу, Прийняв=МВО to-складу, Фін=глобальний фін; приймання ззовні Здав=контрагент); міграція 025 drop `sender_id/receiver_id/fin_id`. (2b) Імпорт переміщень читає МВО з колонок J(звідки)/K(куди) → будує журнал складів діапазонами (зміна закриває попередній період; персони мають існувати, незнайдені — у звіт `mvo_created`/errors; перебудова per-warehouse, реімпорт без дублів). (2c) Форма документа: пікери підписантів прибрано → read-only (snap-імена для підписаного, підказка для чернетки). Тести: e2e API (snap із журналу + **історичний резолв по даті** + імпорт J/K діапазони) + UI-smoke. Хелпер тестів `closeActiveFin` (фін-синглтон). Гілки `feat/mvo-in-documents`, `feat/mvo-doc-form`, `feat/mvo-import-jk`.

22. МВО в документах — Задача 1: глобальний Фін-МВО. У журнал `mvo` додано `kind` ('warehouse'|'fin'), `warehouse_id` став nullable (міграція 024). Фін-МВО — **загальний на всю систему** (не по службі), один діючий, версіонований по даті; видимий **усім** (list_mvo не скоупиться). Керування — Довідники → МВО: у формі «Тип» (Склад / Фінслужба (загальна)); для фін склад не потрібен. `create_mvo` розгалужено (fin: без складу, «один діючий фін»; warehouse: як було — служба/внутр.підрозділ, не зовнішній). Тести: e2e API (фін глобальний/single-active/ротація/видно service-юзеру) + UI-smoke. Задача 2 (далі): Здав/Прийняв/Фін у документах з журналу по даті + імпорт J/K + drop FK. Гілка `feat/fin-mvo`.

21. «Історія» для несерійного майна на «Залишках» (як у серійного). Логіка вже підтримувала (`itemHistory(nomenclature_id, instance_id)` — для несерійного `instance_id` порожній → історія по номенклатурі: рухи + видачі/повернення); бракувало лише кнопки — знято гейт `r.kind === 'serial'`, «Історія» тепер на всіх рядках. Тест: UI-smoke (несерійний рядок → історія показує «видано»). Гілка `feat/nonserial-history`.

20. Тип обліку (облік/ндм) — цілісність. (1) `is_official` — властивість КАРТКИ, денормалізована на екземпляри/рухи/видачі (баланс рахується по `(склад,ном,is_official)`). Зміна на картці не оновлювала копії → розсинхрон. Тепер `PUT /nomenclature`: якщо по картці вже є рухи/видачі — зміну `is_official` **заборонено** (400 «створіть нову картку»), щоб не розʼїхались баланси/видачі; поки обігу нема — дозволено + **каскад на екземпляри**. (2) Прихід: випадайка номенклатури фільтрується за формою — «без документа»→лише НДМ, «накладна/акт»→лише облік; «+ нова» створює картку відповідного типу (`defaultOfficial` у NomenclatureModal); зміна форми скидає вибрані позиції. Тести: e2e API (зміна до обігу+каскад / 400 після руху / не-official-правки працюють) + UI-smoke (фільтр приходу). Гілка `feat/lock-is-official`.


19. НДМ (не облікове майно) — підхід B + фільтр (без нового типу складу). (1) Прихід **без документа**: у «Прийняти майно» форма «Без документа (НДМ)» → `/receive` створює лише рухи receipt (is_official з картки), без custody-документа (константа `RECEIPT_NO_DOC`, повертає `{no_document:true}`). (2) Видача НДМ **напряму зі складу служби будь-якій особі** (без прив'язки до підрозділу): `issue_row` — якщо `is_official=False` і склад = service → дозволити, будь-яка особа; ACL admin/своя-служба; облік зі службового складу — заборонено (лише склад підрозділу). На «Залишках» кнопка «Видати»/«Повернути» тепер і на службовому складі для НДМ (`canIssue/canReturn`), пікер осіб = усі особи для служби; видачі вантажаться і для службового складу. (3) Фільтр **все/облік/ндм** на «Залишках» і «Майно». Тести: e2e API (прихід без doc + видача НДМ будь-кому + гард облік) + UI-smoke (фільтр + пряма видача). Гілка `feat/ndm-support`.

18. «Майно» для service-юзера: приховано дропдаун вибору служби (і колонку «Служба») — не-admin бачить лише свою службу. Бекенд: `GET /api/nomenclature` тепер скоупиться (`scope_nomenclature` в acl: service → `service_id == user.service_id`, admin/інші → все); `/custody/totals` теж (service бачить лише свої nom-id). Раніше service-юзер бачив усю номенклатуру + міг фільтрувати по чужих службах. Тести: e2e API (service бачить лише свою номенклатуру+totals, admin — все) + UI-smoke (нема `.sel`, лише своя позиція). Гілка `feat/catalog-service-scope`.

17. Аудит-лог (журнал усіх змін). Автоматичний — SQLAlchemy `after_flush`-хук пише кожну ORM create/update/delete у `audit_log` (майбутні функції логуються самі, не треба памʼятати). Модель diff повний: `{поле: [було, стало]}`; діючий користувач із `session.info` (кладеться в `get_current_user`); username — знімок (переживає видалення). Трекаються: номенклатура/instances/рухи/видачі/документи + довідники (служби/підрозділи/групи/склади/особи/МВО) + користувачі (без hashed_password). Імпорт — по рядку (автоматично). Bulk `.delete()` (wipe) не логується (свідомо). Сторінка **«Журнал змін»** (admin): фільтри сутність/дія/дата, розкриття рядка → diff. Ендпоінт `GET /api/audit` (+`/meta`), міграція 023. Нюанс: `entity_id` беремо з PK-атрибута (`inspect().identity` порожній одразу після flush). Тести: e2e API (create/update diff + рух + 403 не-admin) + UI-smoke. Гілка `feat/audit-log`.


16. Видача при переміщенні + прибрано «+ Рух». «+ Рух» дублював «Додати переміщення»(transfer)/«Прийняти майно»(receipt), унікальним було лише списання (не потрібне) — кнопку прибрано. У «Додати переміщення» на кожен рядок — опційний **пошуковий пікер особи** (`ItemAutocomplete`, показується лише коли призначення = склад підрозділу; особи фільтруються по цьому підрозділу; при зміні складу скидається). Обрана особа → одразу видача (assignment) в ТІЙ САМІЙ транзакції, що й рух (атомарно; якщо особа з іншого підрозділу — відкат усього). У накладну особа НЕ пише (окремий шар). Бек: логіку видачі винесено в переюзну `issue_row()` (без commit), нею користуються і `POST /assignments`, і пакетний рух; `DocumentItemIn.assign_person_id`. Тести: e2e API (видача+атомарний відкат) + UI-smoke (пікер+видача, «+ Рух» відсутній); оновлено `stock_ui`/`assignments_ui` під нові флоу. Гілка `feat/transfer-with-issue`.

15. Форма чернетки документа: кнопка «+ додати позицію» — докидати рухи в існуючу чернетку. Відкриває модалку з рухами `document_id IS NULL` того самого напрямку (`from/to` = напрямок документа), яких ще немає в позиціях; вибір → додаються локально → «Зберегти» (PUT) прив'язує. Після «Підписати» — заморожено (як і решта). Бек уже вмів (`PUT` з `movement_ids`, `_apply_group` вимагає єдиний напрямок) — зміна фронтова (DocumentFormPage). Тести: e2e API (докидання same-direction + гард різних напрямків) + UI-smoke. Гілка `feat/draft-add-position`.

14. Відкликання непроведеного руху (модель A). Пакетна тижнева накладна вже працює («Без документа» → «Створити документ»). Проблема була в реверсивності: рух проводиться одразу, а видалення позиції з чернетки лише відв'язувало папір — сам рух (факт+історія) лишався. Додано `DELETE /api/custody/movements/{id}` для НЕПРОВЕДЕНОГО руху (`document_id IS NULL`): видаляє рядок і відкочує розміщення (серійне — перерахунок `current_warehouse` з решти рухів по `doc_sort_key`, лише останній рух екземпляра; несерійне — блок, якщо баланс став би від'ємним). Запис зникає з історії (варіант 1, «чисто», без сліду; повний аудит — окрема фаза). Фронт: кнопка «Відкликати» на «Без документа» (DocumentsPage). Тести: e2e API (відкат+гарди: не-останній, у документі) + UI-smoke. Гілка `feat/revoke-undocumented-movement`.


13. Імпорт → кнопка «Очистити людей» (поряд з «Очистити інвентар»). Видаляє список осіб для повного переналаштування. Особи, привʼязані до user-логіну (МВО), НЕ видаляються (інакше логін втратить auto-scope на підрозділ) — у звіті `kept_linked_to_users`. Якщо є видачі (assignments) — блокує з підказкою «спершу очистіть інвентар» (щоб не втратити видачі каскадом). Ендпоінт `POST /api/admin/v2/wipe-persons` → `{deleted_persons, kept_linked_to_users}`. Тест e2e (gated `RUN_WIPE_TEST`): блок за наявних видач → wipe інвентар → лінкована особа лишається, проста та імпортована видаляються. Гілка `feat/wipe-persons`.

12. Імпорт переміщень: тип операції тепер читається з колонки **Z (Запасне поле2)** — «надходження» → receipt (склад-звідки=None, «Звідки» → counterparty), «внутрішнє переміщення» → transfer; форма документа — з колонки **Y (Тип документа)** — «Акт …» → акт, інакше «накладна». Раніше тип угадувався за наявністю from/to, тож надходження ззовні з контрагентом у «Звідки» помилково ставало внутрішнім переміщенням і плодило фантомний склад. Порожня Z → стара евристика (зворотна сумісність). У звіт додано лічильник `classified_by_column`/`by_heuristic` — щоб побачити розбіжність колонок при реімпорті. Хелпери `_op_from_hint`/`_form_from_hint`; тести pytest + e2e. Гілка `feat/import-operation-from-columns`.

11. «Очистити інвентар» (v2 wipe) тепер видаляє й `custody_documents`. Раніше wipe чистив видачі/рухи/екземпляри/номенклатуру, але документи (створюються backfill'ом під час імпорту «Переміщення») лишалися сиротами й накопичувалися між реімпортами. Додано `custody_documents` у видалення + лічильник відповіді. Тест e2e (gated `RUN_WIPE_TEST`, деструктивний): імпорт → документи є → wipe → документи й номенклатура порожні. Гілка `fix/wipe-documents`.

10. Імпорт-3 (баг «екземпляр не на тому складі»): рухи серійного в одну дату розміщувалися за порядком рядків у файлі («останній рядок перемагає»), тож при кількох накладних одного дня екземпляр міг опинитися не на тому складі. Тепер розміщення й сортування історії — детерміновані, за хронологічно ПІЗНІШИМ рухом: ключ `(дата, номер накладної, id)`, де номер розбивається на числові групи (`596/250/2/2` > `596/250/2/1`, числове, не лексичне). Хелпер `doc_sort_key` у `custody_snapshot.py`; розміщення рахується в кінці імпорту (не в циклі); історія (`/custody/history`) сортується тим самим ключем. Тести: pytest на `doc_sort_key` + e2e (файл із перевернутим порядком рядків → перемагає пізніша накладна). Гілка `fix/import-same-date-order`.

1. "Видати в підрозді" перейменуй в "Додати переміщення". Я не дуже розумію в чому різниця між цієї кнопкою і кнопкою "+Рух"

2. Модалка «Додати переміщення»: (1) пошуковий дропдаун майна (довгі назви не вилазять); (2) без дублів — той самий екземпляр не додати двічі (зайняті екземпляри зникають зі списку в інших рядках), несерійну картку не додати двічі.

3. Залишки-Модалка Додати переміщення: у дропдауні лише майно, наявне на поточному складі (несерійне з вільним залишком; серійне з невиданими екземплярами; видане/відсутнє не показуємо, кількість обмежена вільним залишком). + На «Залишки» додано поле пошуку (по назві/серійному, «/» фокусує), працює разом із фільтрами Все/На складі/Видане.

4. Ціна майна — зберігається на картці (nomenclature.price). Форму картки винесено в один спільний компонент: «Майно» (створення/редагування) і «Прийняти майно → + нова номенклатура» тепер використовують ОДНАКОВУ форму з усіма полями (включно з ціною). Раніше на прийманні була скорочена форма без ціни.

5. Підрозділи внутрішні/зовнішні: додано ознаку «зовнішній» (Довідники → Підрозділи, чекбокс + редагування підрозділів; чип «зовнішній»). Зовнішні = джерело приймання; внутрішні = переміщення/облік. Селектор складу на «Залишки» замінено на кнопки-перемикачі (служби + внутрішні підрозділи; зовнішні сховані; без агрегату «Всі склади»). Напрямки переміщення виключають зовнішні. «Прийняти майно → від кого» = дропдаун зовнішніх підрозділів. Наявні підрозділи = внутрішні (перемикаєш вручну). Міграція 021.

6. Примітка на екземплярі серійного майна: додано поле instances.note (міграція 022) + PUT-ендпоінт. На «Залишки» серійні рядки мають редаговану колонку «Примітка» (зберігається одразу); несерійні — «—». Виводиться також у /custody/serial і «Де знаходиться». Редагувати можна прямо на «Залишки».

7. «Прийняти майно»: пікер номенклатури замінено на такий самий пошуковий autocomplete, як у «Додати переміщення» (не вилазить за модалку). Показує весь каталог (приймання = завозимо нове). «+ нова номенклатура» — окрема кнопка «+ нова» в рядку (та сама форма картки). Без дедупу.

8. Імпорт видач — 3-й крок «Видачі» на сторінці Імпорт (той самий файл Items). З колонки «Де» формату «<підрозділ> <Прізвище>» (підрозділ — відомий внутрішній, найдовший префікс; решта = прізвище) створює видачу для майна, вже розміщеного Переміщеннями: серійне — по № картки; несерійне — по номенклатурі на склад підрозділу. Особа — по прізвищу в межах підрозділу; якщо особи нема — СТВОРЮЄТЬСЯ (find-or-create, лічильник persons_created); неоднозначне прізвище → пропуск+звіт. К-сть = 1. Дата = дата накладної руху розміщення (fallback — сьогодні). Невідповідності пропускаються й звітуються. Ендпоінт /api/admin/v2/import/assignments.

9. Звіти → звіт «Видане»: два таби — «Видане на особу» (вибір особи → її активні видачі: майно, серійний, тип, к-сть, од., склад, дата) і «Видане на групу» (вибір групи → по кожному бійцю). Admin бачить усі служби. Бекенд: /assignments збагачено іменами (nomenclature_name/uom/serial_no/warehouse_name), group-holdings — person_name+serial_no. Сторінка /reports (замість заглушки).
